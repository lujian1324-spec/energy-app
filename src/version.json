{
  "version": "4.9.44",
  "build": 2870,
  "date": "2026-09-10",
  "changelog": [
    "Signing in to an existing account sends a sign-in code again, not a sign-up one. The code's purpose was being sent as text where the server expects a number, so it fell back to its default — which is sign-up — and the app was also choosing that purpose from a check whose meaning is documented nowhere."
  ]
}
